#!/bin/bash


# Display Power Management Setting

xset dpms force off 	# Will turn off monitor
xset dpms force on     	# turn back on
                       	#
xset +dpms              # If not work, turn service on